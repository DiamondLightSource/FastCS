apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fastcs.fullname" . }}-test-connection"
  labels:
    {{- include "fastcs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fastcs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
